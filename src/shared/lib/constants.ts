/**
 * Application-wide constants
 */

/**
 * Number of minutes of vessel ping history to fetch and display
 * Used by both VesselPingContext (data fetching) and useVesselLinesData (filtering)
 */
export const VESSEL_HISTORY_MINUTES = 20;
